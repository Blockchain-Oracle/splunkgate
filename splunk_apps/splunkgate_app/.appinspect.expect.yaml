# SplunkGate AppInspect expected findings — none currently suppressed.
#
# Per ADR-008 and story-app-01, SplunkGate ships:
#   - zero binary blobs in bin/ (the DNS Guard winning pattern)
#   - zero Python files in bin/ (mod-input shape, not custom commands)
#   - the icon PNGs in static/ are placeholders generated by Pillow and
#     are first-class app assets, NOT binary blobs that AppInspect's
#     check_for_binary_files_without_source_code flags as inappropriate
#
# When a future story adds a binary that AppInspect flags, append an
# entry of the form:
#
#   check_<name>:
#     comment: 'why this entry is justified — specific enough that a
#       Splunkbase reviewer does not need to read source to evaluate'
#
# CIMplicity precedent: their single suppression suppressed a
# _lsprof.so file required for Python profiling.
{}
